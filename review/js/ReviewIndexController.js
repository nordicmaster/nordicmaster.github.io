class ReviewIndexx extends React.Component {
	constructor(props) {
	  super(props);
	  this.state = { data: [] };
	}
	
	componentDidMount() {
		// fetch artist names from files, also score
	}

        render() {
		          //for each artist - num of reviews, avg score
        }
    }

ReactDOM.render(<ReviewIndexx />,document.getElementById("ReviewController"));
